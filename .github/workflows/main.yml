on:
  push:
    branches:
      - master






jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "login to Docker hub"
        run: docker login --username yousefnagy1 --password dckr_pat_sqvSv2_uVdNgcJt5ZPJuWodkmjs